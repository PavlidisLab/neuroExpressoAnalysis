library(devtools)


library(httr)
set_config( config( ssl_verifypeer = 0L ) )


library(RCurl)
eval( expr = parse( text = getURL(
    "https://raw.githubusercontent.com/oganm/R.andom/master/aggressive.R",
    ssl.verifypeer=FALSE) ))